import { type Handler } from 'innet';
import { type UseComment } from '../../types';
export declare function getComment(handler: Handler, name: string, freeParent?: boolean, parent?: import("../../types").ParentElements): UseComment;
