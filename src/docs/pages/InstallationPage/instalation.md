# Installation
---

Get started with `innetjs` CLI or install the package directly.

```shell
//! CLI
npx innetjs init my-app -t fe

//! npm
npm i innet @innet/dom

//! yarn
yarn add innet @innet/dom
```
